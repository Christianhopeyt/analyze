{
  "name": "youtube-analyzer-site",
  "private": true,
  "scripts": {
    "test": "node --test --test-isolation=none tests/*.test.js",
    "build:fr": "node scripts/generate-fr-site.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0"
  }
}
